{% if view == 'show' %}
    {% if value|length > 0 %}
        <ul class="template_imageArray_imageContainer">
            {% for element in value %}
                <img src="{{ element }}">
            {% endfor %}
        </ul>
    {% else %}
        <div class="empty collection-empty">
            {{ include(entity_config.templates.label_empty) }}
        </div>
    {% endif %}
{% else %}
    {{ value|join(', ')|easyadmin_truncate }}
{% endif %}
